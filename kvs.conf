# KVS RDB 配置文件
# 格式: key = value
# 注释以 # 开头

# 自动保存间隔（秒）
save_interval = 900

# RDB 文件路径
rdb_file = kvs.rdb

# 是否启用 RDB
rdb_enabled = yes

# 是否启用压缩（可选）
rdb_compression = no

# 是否启用CRC64校验
rdb_crc64 = no

# 是否启用备份
rdb_backup = yes

# 备份目录
backup_dir = ./backup 